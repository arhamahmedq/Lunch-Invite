# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Env / secrets (never commit API keys)
.env
.env.local
*.env

# Node (if you ever add a build step)
node_modules/
npm-debug.log*

# Audio file — too large for git, host separately (see README)
assets/audio/
